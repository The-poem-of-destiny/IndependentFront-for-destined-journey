# 更新日志

本文件记录面向玩家的版本变更。

---

## 2026-07-30

### 生成质量：物品与角色

- **物品 / 装备 / 技能的描述大幅精简。** 此前 AI 生成的物品描述动辄两百字往上、长到面板里根本显示不完（一把钥匙能被写成一整段散文）。现已约束为默认 100 字以内、硬上限 120 字——该说的说清，没用的感官堆砌砍掉。
- **修复：主角一次获得多件装备时，部分装备会"丢失"。** 此前调度 AI 偶尔会把好几件新装备笼统塞进一条角色状态更新里，导致它们永远不被真正生成、进不了背包。现已强化为：每件装备各自单独走生成流程。
- **命定核心 / 命定系统化身 这类抽象存在体不再被生成一堆用不上的技能和装备。** 它们是世界设定的化身或意志，没有实体、不参与战斗——给它们造技能和装备纯属浪费。

---

## 2026-07-29

### 战斗系统重做（v2）

战斗系统经历了一次从头重构，为后续更丰富的战斗效果打基础。

- 更结构化的效果与状态（buff）体系，覆盖检定 / 伤害 / 防御 / 资源等大类
- 战斗进行时有独立的战斗面板，行动选项直接注入对话框，可点击操作
- 战斗触发统一由叙事调度处理，与制作、角色生成等子系统解耦

> 这一版以引擎与面板为主，完整的真机体验仍在打磨。

### 设置页：API 配置更稳了

集中修了一波 API 配置页的问题：

- 连接测试与模型列表获取更鲁棒（自动归一化地址、去重模型、明确的错误提示）
- 模型输入支持手动填写（不再被下拉列表绑架）
- 出厂预设的名称与配置文件保持同步

---

## 2026-07-27

### 🎵 音频系统 v1.0

游戏现在有配乐了。

- **地点变化自动换 BGM**：走到不同区域，背景音乐跟着切换；切回首页 / 进游戏等界面切换也会换曲。
- **内置曲库**：收录 57 首曲目（35 地点 A/B 双轨 + 13 通用场景 + 9 人物主题），按地点 / 人物 / 情绪 / 情境四维标签自动选曲。素材作者 Aoo。
- **混音台**：主音量 / 音乐 / 音效独立调节，支持静音、循环、随机、进度控制。
- **本地音乐文件夹**：选一次目录，文件留在磁盘上不入库；每次开浏览器授权一次即可。也支持直接上传到浏览器存储。
- **播放列表**：自己编排曲目顺序，支持拖拽排序。
- **场景配乐开关**：不想自动换曲可以关掉，地点照常记录。

> ⚠️ **全新下载的版本，内置曲库会显示 57 首但点不响**——原始 mp3 文件因体积已移出代码仓库。把 mp3 文件放回 `public/audio/bgm/` 目录即可恢复，无需改代码。
> 另：浏览器不允许零交互自动播放音频，进入游戏后的第一次点击 / 按键会激活声音——这是浏览器平台的限制，不是 bug。

---

## 2026-07-24

### 存档与回退（新功能）

今天加了一套"后悔药"——走错路、抽卡歪了、或者单纯不满意 AI 这轮的发挥，都能往回退。

- **最新一条 AI 回复上右键 →「回退本轮」**：撤回这一回合，并把**你这轮的输入原样回填到输入框**。
  - 直接再按发送 = 用同样的输入重新生成（AI 重跑一遍，产出新结果）。
  - 改几个字再发送 = 编辑后重发。
  - 一个动作同时覆盖「重新生成」和「编辑重发」两种需求。
- **最新一条 AI 回复上右键 →「复制」**：复制该条消息正文。
- **左侧工具栏「快照」按钮**：打开历史快照面板，可恢复到之前任意一个保存点——对话、角色状态、记忆一并回滚到那个时刻。战斗进行中暂禁用。
- **快照更聪明地保留**：最近几轮全部保留（保证随时能回退本轮），再往前的快照按阶梯稀疏保留，既不占太多空间、又能回到更早的关键节点。设置里可切换「阶梯式（推荐）」或「密集（每轮都留）」。

### 叙事 AI 上下文修复

- **修复：叙事 AI 现在能正确收到你的输入、角色状态与世界设定。** 此前故事预设里的一套占位符（玩家输入 / 角色状态 / 场景世界书 / 相关记忆 / 对话历史）因渲染管线漏了一层而全部"裸奔"成字面文本——AI 实际拿到的上下文是残缺的，可能导致叙事与你的操作、当前状态脱节。现已修正，预设内的占位符会就地渲染成真实数据。
- **顺手优化了提示词缓存命中。** 此前每轮记忆召回结果一变，就会从提示词很靠前的位置打断缓存，导致后面整段（含数万字世界设定）全部缓存未命中——既慢又重复计费。现在动态数据收敛到预设中部的对应区块，前半段静态内容稳定命中缓存，响应更快、更省。

---

## 2026-07-23

### 世界书管理

- **条目编辑器新增「全部启用 / 全部禁用」按钮**——一键批量切换某本书的所有条目，不用再逐条点。
- **修复：禁用某条目后该行按钮全部失灵**（连重新启用都点不了）——根因是全局样式冲突，已修正。
- **修复：禁用条目现在对所有 Agent 真正一致生效**。此前因兼容旧格式遗留的「永久注入」标记会绕过启用开关，导致你禁用的角色 / 设定仍被偷偷注入大纲等内容；现已移除，启用开关是唯一标准。

---

## 2026-07-21

### 剧情系统重大升级

今天最大的改动——剧情生成从「线性剧本」全面转向「开放世界」。

- **世界自行运转**：AI 现在构造的是一个会自己运转的世界。各方 NPC 有自己的目标和行动轨迹，他们不围着主角转——大部分 NPC 甚至根本不认识你。世界按自己的逻辑演化。
- **主角自由介入**：不再有预设的 A/B/C 分支或必经的关卡。你想做什么就输入什么，世界会回应你的行动。
- **不介入会怎样**：AI 为每个大事件设定「主角完全不介入时世界会怎样」的演化基线。你的影响 = 你改变了什么。粉碎一条势力线，会自然涌现新的权力真空与势力博弈。
- **偏好是结果方向，不是任务清单**：你的自定义偏好（如「后宫」「成长」）现在被理解为**期望达成的结果**，而不是主角必须依次完成的任务。AI 构造一个有可能涌现这个结果的世界，走不走得到由你的选择决定。
- **命定核心融入世界**：如果你在捏人时选了命定核心（如命定之书），相关势力、外部存在、引发的态势会自然融入世界态势，而不是写成主角的专属剧情。

### 稳定性修复

- **剧情大纲生成频繁超时失败**（重 roll 多次仍报错）→ 已修复：超时时间从 2 分钟提到 5 分钟；失败提示从看不懂的英文报错改为清晰的「请求超时，请重试或减少上下文注入」
- 捏人页在深色主题下部分按钮不可见 → 修复
- 捏人确认页 HP / MP / SP 资源条把数值吞掉 → 改为清晰的数字显示
- 预设弹窗：预设过多时被压扁、操作按钮被裁掉 → 修复
- 预设详情面板：标签被挤到只剩一个字 → 修复

### 捏人页体验改进

- 剧情规划页新增「清除大纲」入口——可重新生成大纲，不影响已捏好的角色
- 预设弹窗每个预设新增「覆盖」按钮——一键用当前配置覆盖该预设
- 捏人各步骤页面宽度统一——翻页时不再忽宽忽窄

### 文档

- **新建 [README](README.md)**——面向玩家的快速开始与首次配置指南：如何创建 3 个 API（DeepSeek Flash + Pro + 硅基流动 Embedding）、如何绑定 Agent 模型、如何开始第一局游戏

---

_更早的变更可查看 [提交历史](../../commits/master)。_
