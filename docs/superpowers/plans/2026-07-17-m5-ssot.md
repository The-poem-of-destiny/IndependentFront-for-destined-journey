# M5 SSOT 落地 实施计划（数据字段规范批次 5）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 铁律 4（每类数据唯一真源）全面落地：变量迁入 SaveProfile.variables、Snapshot 按规范 §11.2 重定义（打快照=整份深拷贝 / 恢复=覆写+对话回滚）、新闻/好感度翻译接线，外加 totalTurns/focusQuest/weekday/gameStartTime 四项杂修。

**Architecture:** 先迁变量（依赖最少），再做快照大手术（类型重定义 + 打/恢复机制 + 触发点迁到 GamePipeline），最后接新闻/好感翻译与杂修。M2 已就位的 op（add_news/set_affection/delta_affection）在本批接上翻译层。执行前与 M2-M4 实际完成状态校准。

**Tech Stack:** TypeScript · Dexie/fake-indexeddb · Vitest · Vue3/Pinia（GamePipeline 触发点）

## Global Constraints

- 每个 task 完成后 `npm run typecheck` 0 错误 + `npm run test -- --run` 不新增失败（已知 1 失败: create-store 命定之灵）。
- 本计划在 M4 完成后执行。行号为快照，执行时按符号锚点定位。
- types.ts 唯一类型来源；注释中文；commit 中文前缀。
- 规范: `docs/superpowers/specs/2026-07-16-data-field-conventions-design.md` §8/§11/§12/§13；52 项: `docs/superpowers/specs/2026-07-16-entity-field-audit.md`。
- M1 终审备忘（本批履约）: SaveProfile.variables 读方接管前必须做加载归一化 `variables ??= {}`。

---

### Task 1: 变量迁家 — SaveProfile.variables 接管读写

**Files:**
- Modify: `src/sillytavern/state-manager.ts`（getCurrentVariables / persistVariables + 五个 variable op handler 的落点）
- Modify: `src/sillytavern/save-profile.ts`（getProfile 加载归一化）
- Test: `src/sillytavern/state-manager.test.ts`

**Interfaces:**
- Produces: `getCurrentVariables(): Promise<Record<string,any>>` 改读 `(await getProfile(saveId)).variables`；`persistVariables(vars)` 改 `updateProfile(saveId, p => { p.variables = vars })`；**寄生快照路径全删**（读最新快照/原地改快照的两个函数体整体替换）。set/delta/remove/move/insert_variable 五 op 形状不变、落点变（杀 #1 静默丢弃 + #33 双轨）。
- getProfile 内 `if (profile.variables === undefined) profile.variables = {};`（存量记录归一化）。

- [ ] **Step 1: 失败测试** — ① set_variable 后 getProfile 的 variables 可读到（不再依赖快照存在）② **无快照时写入不丢**（旧实现静默丢弃的反例转正例）③ delta_variable 在 profile 上累加 ④ user./sys. 命名空间路径不变。
- [ ] **Step 2: 确认失败**；**Step 3: 实现**；**Step 4: 既有变量断言更新（原依赖 mock 快照的用例改依赖 profile）+ typecheck + 全量**。
- [ ] **Step 5: Commit** `feat(M5): 变量唯一真源迁入 SaveProfile.variables (#1 #33)`

覆盖: #1 #33。

---

### Task 2: Snapshot 类型重定义 + 打快照

**Files:**
- Modify: `src/sillytavern/types.ts`（Snapshot 接口整体替换）
- Modify: `src/sillytavern/state-manager.ts`（createAutoSnapshot 替换为 createSnapshot；patchCount%5 死逻辑删除）
- Modify: `src/sillytavern/database.ts`（snapshots 表 schema 若索引字段变化则 v10；trimSnapshots 上限改读 settings）
- Test: `src/sillytavern/state-manager.test.ts` / `database.test.ts`

**Interfaces:**
- Produces（规范 §11.2）:

```ts
export interface Snapshot {
  id: string;                       // Code 生成 UUID
  saveId: string;                   // 一等字段
  createdAt: number;                // 现实时间戳
  reason: 'turn' | 'manual' | 'pre-combat';
  turn: number;                     // 对话回合游标（恢复时截断 messages 用）
  characters: CharacterState[];     // 深拷贝
  saveProfile: SaveProfile;         // 深拷贝（任务/时间/好感/变量随行）
}
```

（旧 index/timestamp/variables/messageIds/memoryIds/plotEvents/gameTime 字段删除——typecheck 清单驱动清理消费点。）
- `createSnapshot(reason, turn): Promise<Snapshot>` — `getCharacters(saveId)` + `getProfile(saveId)` 深拷贝（`structuredClone`）落 snapshots 表 + `save.activeSnapshotId` 指向 + `trimSnapshots(saveId, settings.maxSnapshotsPerSave ?? 30)`。
- StateManager 的 `patchCount % 5` 自动快照逻辑与 maxSnapshots 私藏常量删除（杀 #28，顺带 #28 的即建即抛失真消解）。

- [ ] **Step 1: 失败测试** — ① createSnapshot 后表里的快照 characters 非空、saveProfile.variables 随行 ② 深拷贝隔离（改原对象不影响快照）③ 超上限滚动删除最旧 ④ commitChatState 不再自动产生快照。
- [ ] **Step 2: 确认失败**；**Step 3: 实现**（v10 仅在需要新索引时升——`'id, saveId, createdAt'`；旧 snapshots 开发数据允许清弃，upgrade 直接 clear 该表并注记）。
- [ ] **Step 4: 断言更新 + typecheck + 全量**；**Step 5: Commit** `feat(M5): Snapshot 重定义 — 整份深拷贝 + 滚动上限读 settings (#2 #28 前半)`

覆盖: #2（写侧）#28。

---

### Task 3: restoreSnapshot — 覆写 + 对话回滚

**Files:**
- Modify: `src/sillytavern/state-manager.ts`（新增 restoreSnapshot）
- Modify: `src/sillytavern/database.ts`（deleteMessagesAfterTurn(saveId, turn) — 用 [saveId+turn] 复合索引，杀 #49 死索引）
- Test: `src/sillytavern/state-manager.test.ts`（完整集成测试）

**Interfaces:**
- Produces: `restoreSnapshot(snapshotId): Promise<StateCommitResult>` — ① 按 id 读快照（saveId 校验防跨档）② characters: 当前存档全删后 bulkPut 快照副本（整体覆写语义）③ saveProfile 覆写 ④ `deleteMessagesAfterTurn(saveId, snapshot.turn)`（对话跟着回滚）⑤ `save.activeSnapshotId` 更新 ⑥ 失败进 errors[]。

- [ ] **Step 1: 失败集成测试（必写全）**：

```ts
it('restoreSnapshot: 状态覆写 + 对话回滚到快照 turn', async () => {
  // 造 3 轮对话消息(turn 1/2/3) + turn2 时打快照(角色 hp=80, fp=5)
  // → turn3 后角色 hp=30、fp=9、新增消息
  // → restoreSnapshot
  // 断言: hp 回 80、fp 回 5、turn3 消息已删、turn1/2 消息还在、variables 随 profile 回滚
});
```

- [ ] **Step 2: 确认失败**；**Step 3: 实现**；**Step 4: 通过 + typecheck + 全量**。
- [ ] **Step 5: Commit** `feat(M5): restoreSnapshot — 整体覆写 + 按 turn 截断对话 (#2 恢复侧 #49)`

覆盖: #2（恢复死路径根治）#49（复合索引启用）。

---

### Task 4: 快照触发点 — GamePipeline 每轮一拍 + totalTurns 语义

**Files:**
- Modify: `src/ui/lib/game-pipeline.ts`（run() 管线成功后）
- Modify: `src/sillytavern/state-manager.ts`（commitChatState 的 totalTurns+1 行删除）
- Modify: `src/ui/components/home/HomePage.vue`（Lv.{{totalTurns}} 误用改为存档回合数展示语义修正）
- Test: `src/ui/stores/game-store.test.ts` / `state-manager.test.ts`

**Interfaces:**
- Produces: GamePipeline.run() 成功路径末尾（refreshFromDb 前）: `await sm.advanceTurn()`——新增的 StateManager 方法: `totalTurns += 1` 落 save + `createSnapshot('turn', currentTurn)`。commitChatState 不再动 totalTurns（杀 #27 每 commit 虚高）。

- [ ] **Step 1: 失败测试** — ① 一次管线（多次 commit）后 totalTurns 恰 +1 ② advanceTurn 产生 1 个 reason='turn' 快照 ③ commitChatState 单独调用不改 totalTurns。
- [ ] **Step 2: 确认失败**；**Step 3: 实现**；**Step 4: 断言更新（原"每 commit +1"用例反转）+ typecheck + 全量**。
- [ ] **Step 5: Commit** `feat(M5): 回合快照触发 + totalTurns 每轮+1 (#27)`

覆盖: #27 + 快照触发机制。

---

### Task 5: 新闻/好感度翻译接线

**Files:**
- Modify: `src/sillytavern/agent-orchestrator.ts`（Stage2 世界新闻→add_news；Stage3 affections 键→set/delta_affection）
- Modify: `src/sillytavern/namespace-normalizer.ts`（sys.news 映射删除）
- Test: `src/sillytavern/agent-orchestrator.test.ts`

**Interfaces:**
- Consumes: M2 op（add_news/set_affection/delta_affection）+ M4 prompt 教的键格式。
- Produces: dispatcher `<json>` 的世界新闻字段（现走 set_variable 世界新闻路径）改产 `add_news {title, content, category}`；vars_update `<json>` 的 `affections.set/delta` 数组 → 对应 patch；变量命名空间的「世界新闻」「关系列表」路径退役（写点删除；旧存档残留变量不迁移，接受陈旧）。

- [ ] **Step 1: 失败测试** — ① dispatcher json 带新闻 → add_news patch 且 profile.news 可读 ② affections.set → set_affection ③ 关系列表变量不再被写。
- [ ] **Step 2: 确认失败**；**Step 3: 实现**；**Step 4: 通过 + typecheck + 全量**。
- [ ] **Step 5: Commit** `feat(M5): 新闻/好感度翻译接线 — 双轨退役 (#15 #16 #44)`

覆盖: #15 #16 #44。

---

### Task 6: 杂修四连 — focusQuest 回写 / weekday / gameStartTime / 遗留清理

**Files:**
- Modify: `src/ui/components/game/QuestsPanel.vue`（focusQuest v-model → 持久化）
- Modify: `src/sillytavern/time-system.ts`（weekday 往返漂移）
- Modify: `src/ui/utils/test-save.ts`（gameStartTime 写游戏时间串 → 现实 ISO）
- Modify: `src/sillytavern/types.ts`（SaveSlot 顶注释"每槽最多 30 快照"→"快照上限见 AppSettings.maxSnapshotsPerSave"）
- Test: `src/sillytavern/time-system.test.ts`（若无则该文件新建仅覆盖 weekday 往返）

- [ ] **Step 1: weekday 失败测试** — `parseGameTime(formatGameTime(t))` 往返后 weekday 不变（现状: 周日→7→'周六' 漂移，#31 实锤在 time-system.ts :50/:67-70/:90 的 1-based/0-based 混用）。
- [ ] **Step 2: 实现三修**（weekday 统一 1=周一…7=周日 或对齐现有 WEEKDAY_NAMES 索引约定，二选一并在注释写死；focusQuest: `watch(focusQuest, v => updateProfile(saveId, p => { p.focusQuest = v }))` 式回写；test-save 改 `new Date().toISOString()`）。
- [ ] **Step 3: 通过 + typecheck + 全量**；**Step 4: Commit** `fix(M5): focusQuest 回写 + weekday 往返 + gameStartTime 语义 (#14 #31 #42)`

覆盖: #14 #31 #42。

---

### Task 7: 收尾 — 附录 A 注记 + 验证

- [ ] 规范附录 A 追加 M5 执行注记（SSOT 总表全绿：变量/快照/新闻/好感全部单源）。
- [ ] `npm run typecheck && npm run test -- --run` 终验；`bash scripts/notify.sh "M5 SSOT 落地 完成!" "变量迁家 | 快照重建 | 新闻好感接线"`。
- [ ] Commit `docs(M5): 附录 A 执行注记`。

---

## 覆盖清单

| # | Task |
|---|------|
| #1 变量寄生快照 | 1 |
| #2 快照双轨死路径 | 2 / 3 |
| #14 focusQuest | 6 |
| #15 好感度恒 0 | 5 |
| #16 新闻双轨 | 5 |
| #27 totalTurns 虚高 | 4 |
| #28 即建即抛+patchCount%5 | 2 / 4 |
| #31 weekday 漂移 | 6 |
| #33 变量双轨 | 1 |
| #42 gameStartTime 双语义 | 6 |
| #44 好感 key 语义分裂 | 5 |
| #49 死复合索引 | 3 |
| M1 终审备忘 variables??={} | 1 |
