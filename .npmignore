# 🔒 P1-18 / P2-07: npm 发布包排除清单
# 发布模式（引擎库 / Web 应用 / 桌面应用）的独立边界待定（review 第五阶段），
# 此处先排除明显不该进入任何发布包的内容：临时文件、备份、crash dump、测试、开发配置。
#
# 🔴 package.json 的 "files" 白名单已于 2026-08-17 删除（它与本清单互斥：两者同时存在时
#    npm 只认 "files"，本文件整份失效）。目前仓库**没有发布流程**，这份黑名单是唯一的清单。
#    真要发布时再裁定用哪一种（黑名单 .npmignore / 白名单 files），别两份都留着。

# 临时 / 备份 / crash dump
tmp/
*.bak
*.stackdump
*.orig
*.pre-*

# 测试源码与 fixtures（不进发布包）
tests/
*.test.ts
*.test.js

# Claude Code 本地配置 / skill
.claude/

# 开发工具脚本
scripts/

# Git 元数据
.git/
.gitignore
.gitattributes

# 编辑器 / IDE
.vscode/
.idea/
